import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    width: 230,
    margin: '0px 5px 5px',
  },
  media: {
    height: 230,
  },
});

export default useStyles;
